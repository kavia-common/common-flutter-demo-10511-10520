# common-flutter-demo

Flutter scaffold for the `common-flutter-demo-10511-10520` container.

## Build (debug APK)

From this directory:

```bash
flutter pub get
flutter build apk --debug
```

Expected output (standard Flutter path):

- `build/app/outputs/flutter-apk/app-debug.apk`
