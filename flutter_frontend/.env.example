# Backend stub base URL for token sync + send request endpoints.
# If not set, the app will still receive pushes, but it will NOT attempt to sync the
# device FCM token anywhere and the "send" stub will be disabled.
#
# Example:
# NOTIF_BACKEND_BASE_URL=https://your-api.example.com
NOTIF_BACKEND_BASE_URL=
